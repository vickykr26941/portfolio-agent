services:
  portfolio-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Hot-reload data file without rebuilding image
      - ./data/portfolio_data.json:/app/data/portfolio_data.json:ro
    restart: unless-stopped